"""(옵션) 무거운 비동기 작업 큐 워커 자리 — 현재 미사용.

지금은 모든 요청을 동기로 처리하므로 필요 없다. 아래가 지연 문제(UX)가 될 때 도입한다:
- 이미지 생성(cover / relationship_map): 모델 호출 10~30초+ → 큐로 빼서 job_id 반환 + 폴링/웹훅
- 소설 전체 번역: LLM 다회 + 리뷰어로 1~3분+ → 비동기 잡 후보

도입 시: Redis(또는 SQS) + arq/Celery 워커. 라우터는 작업 enqueue + 상태 조회 엔드포인트 제공.
미니멀하게 가려면 이 폴더는 제거해도 무방(현재 아무것도 import하지 않음).
"""
